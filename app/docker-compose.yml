version: '3.8'
services:
  finbert-api:
    build: .
    container_name: finbert-api
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - .:/app
    restart: unless-stopped
    depends_on: []
# Add other services (e.g., chromadb) here if needed
